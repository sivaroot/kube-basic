kind: ConfigMap
apiVersion: v1
metadata:
  name: nginx-config
  namespace: kube-basic
data:
  default.conf: |
    server {
      listen 80;
      server_name _;

      location / {
        proxy_pass http://sample-web:8080;
      }
    }